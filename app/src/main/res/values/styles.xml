<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/theme_window_background</item>
    </style>

    <style name="DigitsTheme"
           parent="Theme.AppCompat.NoActionBar">
        <item name="android:textColorPrimary">
            @color/theme_window_background</item>
        <item name="android:textColorSecondary">
            @color/theme_window_background</item>
        <item name="android:windowBackground">
            @color/theme_accent</item>
        <item name="android:textColorLink">@color/theme_window_background</item>
        <item name="colorAccent">@color/theme_window_background</item>
    </style>
</resources>
